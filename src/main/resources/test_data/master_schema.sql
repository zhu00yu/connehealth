CREATE TABLE MASTER (
	ID BIGINT NOT NULL,
	TYPE VARCHAR(255) NOT NULL,
	DATA_KEY VARCHAR(255) NOT NULL,
	DATA_VALUE VARCHAR(255) NOT NULL,
	PARENT_ID BIGINT,
	IS_DISABLED BOOLEAN NOT NULL,
	CONSTRAINT MASTER_PK PRIMARY KEY (ID)
);

CREATE INDEX SYS_IDX_MASTER_PK_10234 ON MASTER (ID);